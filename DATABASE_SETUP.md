# إعداد قاعدة البيانات المحلية - DZ Taxi

## نظرة عامة

تم إعداد تطبيق DZ Taxi ليدعم نوعين من قواعد البيانات:

1. **قاعدة البيانات المحلية (SQLite)** - الافتراضية
2. **Supabase (PostgreSQL)** - السحابية

## الميزات الجديدة

### ✅ قاعدة البيانات المحلية (SQLite)
- **سريعة ومباشرة**: لا تحتاج اتصال بالإنترنت
- **مناسبة للتطوير**: مثالية للاختبار والتطوير المحلي
- **مجانية تماماً**: لا توجد تكاليف إضافية
- **آمنة**: البيانات محفوظة محلياً على جهازك
- **سهلة الإدارة**: نسخ احتياطية يدوية

### ✅ Supabase (PostgreSQL)
- **قاعدة بيانات سحابية متقدمة**: ميزات متقدمة
- **ميزات المصادقة المدمجة**: تسجيل دخول آمن
- **دعم الوقت الفعلي**: تحديثات فورية
- **نسخ احتياطية تلقائية**: حماية البيانات
- **قابلة للتوسع**: تناسب المشاريع الكبيرة

## كيفية الاستخدام

### 1. الوصول لإعدادات قاعدة البيانات
- اذهب إلى: `http://localhost:5174/database-settings`
- أو انقر على "إعدادات قاعدة البيانات" في القائمة الرئيسية

### 2. التبديل بين قواعد البيانات
- اختر "قاعدة البيانات المحلية" للتطوير والاختبار
- اختر "Supabase" للإنتاج والاستخدام الفعلي
- التبديل فوري ولا يتطلب إعادة تشغيل

### 3. إدارة البيانات
- **قاعدة البيانات المحلية**: البيانات محفوظة في `prisma/dev.db`
- **Supabase**: البيانات محفوظة في السحابة

## الملفات الجديدة

```
src/
├── integrations/
│   └── database/
│       ├── client.ts          # Prisma client
│       └── services.ts        # خدمات قاعدة البيانات المحلية
├── hooks/
│   └── useDatabase.ts         # Hook للتبديل بين قواعد البيانات
├── components/
│   └── DatabaseSwitch.tsx     # مكون التبديل
└── pages/
    └── DatabaseSettings.tsx   # صفحة الإعدادات

prisma/
├── schema.prisma              # مخطط قاعدة البيانات
└── migrations/                # ملفات الهجرة
```

## الإعداد التقني

### Prisma Schema
تم إنشاء مخطط شامل يتضمن:
- **Profiles**: ملفات المستخدمين
- **Trips**: الرحلات
- **Bookings**: الحجوزات
- **Vehicles**: المركبات
- **Notifications**: الإشعارات
- **SystemSettings**: إعدادات النظام
- **AdminLogs**: سجلات الإدارة

### خدمات قاعدة البيانات
- **DatabaseService**: خدمات قاعدة البيانات المحلية
- **useDatabase**: Hook للتبديل بين قواعد البيانات
- **دعم كامل**: جميع العمليات (CRUD) مدعومة

## المزايا

### للمطورين
- **تطوير سريع**: قاعدة بيانات محلية سريعة
- **اختبار آمن**: لا تؤثر على البيانات الحقيقية
- **مرونة**: التبديل السهل بين البيئات

### للمستخدمين
- **أداء عالي**: قاعدة بيانات محلية سريعة
- **خصوصية**: البيانات محفوظة محلياً
- **موثوقية**: لا تعتمد على الاتصال بالإنترنت

## الخطوات التالية

1. **اختر قاعدة البيانات المناسبة** من صفحة الإعدادات
2. **ابدأ التطوير** مع قاعدة البيانات المحلية
3. **انتقل للإنتاج** مع Supabase عند الحاجة
4. **استمتع بالتطوير** مع DZ Taxi! 🚗

## الدعم

إذا واجهت أي مشاكل:
1. تحقق من صفحة إعدادات قاعدة البيانات
2. تأكد من تهيئة قاعدة البيانات
3. راجع ملفات السجل في وحدة التحكم

---

**تم تطوير هذا النظام بواسطة فريق DZ Taxi** 🇩🇿
